name: Build on Ubuntu Latest

on: [push]
jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "The job was automatically triggered by a ${{ github.event_name }} event."
      - name: Check out repository code
        uses: actions/checkout@v4
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - name: Install Qt
        uses: jurplel/install-qt-action@v3
        with:
          version: '6.6.3'
          host: 'linux'
          target: 'desktop'
          install-deps: 'true'
          archives: 'qtmultimedia qtqml qtqmlcore qtqmlmodels qtwidgets qtquick qtquickcontrols qtopengl qtcore qtbase qtdeclarative qtsvg qtcmake qtconcurrent qtgui'
          modules: 'qtmultimedia'
          setup-python: 'true'
          set-env: 'true'
          tools-only: 'false'
      - name: Install required libs
        run: |
          sudo apt install ninja-build cmake build-essential libgl1-mesa-dev libicu-dev
          sudo ln -s /usr/lib/x86_64-linux-gnu/libicui18n.so.66 /usr/lib/x86_64-linux-gnu/libicui18n.so.56
          sudo ln -s /usr/lib/x86_64-linux-gnu/libicui18n.so.66 /usr/lib/x86_64-linux-gnu/libicui18n.so.56.1
          sudo ln -s /usr/lib/x86_64-linux-gnu/libicuuc.so.66 /usr/lib/x86_64-linux-gnu/libicuuc.so.56
          sudo ln -s /usr/lib/x86_64-linux-gnu/libicuuc.so.66 /usr/lib/x86_64-linux-gnu/libicuuc.so.56.1
          sudo ln -s /usr/lib/x86_64-linux-gnu/libicudata.so.66 /usr/lib/x86_64-linux-gnu/libicudata.so.56
          sudo ln -s /usr/lib/x86_64-linux-gnu/libicudata.so.66 /usr/lib/x86_64-linux-gnu/libicudata.so.56.1
          find /usr/lib -name "libicui18n.so.*"
          find /usr/lib -name "libicuuc.so.*"
          find /usr/lib -name "libicudata.so.*"
      - name: build 
        run: cmake .; make
